version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:5173" # Map Vite dev server port to localhost
    volumes:
      - .:/app
      - /app/node_modules
    stdin_open: true
    tty: true
